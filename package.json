{
  "name": "simulateur-ptp",
  "version": "1.0.0",
  "description": "Simulateur Interactif PTP v1/v2 - Application éducative pour comprendre le Precision Time Protocol",
  "main": "index.html",
  "scripts": {
    "dev": "npx wrangler pages dev public --port 8080",
    "build": "node scripts/build.js",
    "deploy": "npm run build && npx wrangler pages deploy dist --branch=claude/ptp-simulator-app-01JQmZUm3g1Jy8tSJiv6DCCb",
    "deploy:production": "npm run build && npx wrangler pages deploy dist --branch=claude/ptp-simulator-app-01JQmZUm3g1Jy8tSJiv6DCCb --commit-dirty=true",
    "preview": "npx wrangler pages dev public",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "PTP",
    "Precision Time Protocol",
    "BMCA",
    "IEEE 1588",
    "simulation",
    "éducation"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.47.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
